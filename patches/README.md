# Build-Time Overlay Notes

This directory is intentionally not a patch queue for upstream `raylib`.

Use it for:

- notes about generated overlays
- consumer-side build instructions
- temporary compatibility guidance for specific upstream versions

Do not commit edits that rewrite `raylib/src` in place here.
